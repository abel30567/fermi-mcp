# Shared Types

- **types.ts** — Cross-package TypeScript type definitions shared between the worker and any future packages

Keep types minimal and focused on interfaces that cross package boundaries (e.g., memory records, skill metadata, tool definitions).
